# Notes (L3) — courses

> L3 = how / volatile. AI writes free. Cites L2 IDs up. Matches code as-is.
> `docs/contracts/courses.md` now exists (Task 5). Earlier `@spec` tags in
> this file and in `packages/db/src/schema.ts` that forward-referenced
> `L2-COURSE-01..08` before the contract existed now resolve to real clauses;
> left as-is below rather than rewritten, since the tags themselves didn't
> change, only their target's existence.

## File map
- `apps/web/app/learn/_lib/course-validation.ts` — pure input schemas for authoring, no server imports so both the actions and their test share one set of rules: `courseSchema` (`title` required, trimmed, ≤200 chars; `description` trimmed, blank → `null`), `deckSchema` (`title`, same rule), `cardSchema` (`front`/`back`, required + trimmed, ≤10,000 chars each). Length ceilings are paste-accident guards, not editorial limits. Forward-references `L2-COURSE-05`.
- `apps/web/app/learn/_lib/course-validation.test.ts` — unit tests for the three schemas: title trim + blank-description → `null`, empty-title rejection, over-length rejection (course + card), deck title required, card requires both non-blank sides.
- `apps/web/app/learn/courses/_actions.ts` — course authoring server actions: `createCourse`, `updateCourse`, `setCourseStatus`, `deleteCourse`, plus the shared `ActionState` type. Every action calls `requireTeacher()` itself (never trusts a hidden-UI check) and takes `schoolId` from that call's returned membership, never from a fresh `getCurrentSchoolId()` call — the `L2-SCHOOL-11` rule. Every update/delete proves ownership inside the `WHERE` clause (`eq(courses.id, id), eq(courses.schoolId, teacher.schoolId)`) in the same query that mutates, not a fetch-then-check — a course id from another school matches zero rows and the action returns "Course not found." rather than leaking existence. `deleteCourse` relies on FK cascade (`L2-DB-40..43`) to remove decks/cards/enrolments with the course, and applies identically to a published course with live enrolments and student review history — no extra warning, no distinct path, no confirmation, no undo. Forward-references `L2-COURSE-06`; satisfies `L2-SCHOOL-05`, `L2-SCHOOL-06`, `L2-SCHOOL-11`.
- `apps/web/app/learn/courses/page.tsx` — teacher's course list, server component. Calls `requireTeacher()` and scopes the listing query in the `WHERE` clause (`eq(courses.schoolId, teacher.schoolId)`), same ownership-proof shape as the actions — a course from another school is unreachable, not merely unlinked from the UI. Joins `decks`/`cards` with `leftJoin` + `count(cards.id)` to show a per-course card count without an N+1 query, grouped by `courses.id`. Renders `CourseForm` below the list so creating and browsing live on one page. Forward-references `L2-COURSE-06`; satisfies `L2-SCHOOL-06`.
- `apps/web/app/learn/courses/_components/course-form.tsx` — client component, the new-course form. Calls `createCourse` through `useActionState`; the action re-checks the teacher role and re-scopes to `schoolId` server-side, so this component trusts nothing about the client's authorization state. Plain `<Input>`/`<Textarea>`, no markdown preview — markdown rendering is Task 4's concern.
- `apps/web/app/learn/courses/[courseId]/page.tsx` — course detail, server component. Same ownership-proof shape as the list page and the actions: `and(eq(courses.id, courseId), eq(courses.schoolId, teacher.schoolId))` in one query, `notFound()` on an empty result. A course id belonging to another school 404s exactly like a nonexistent id — it never distinguishes "wrong school" from "doesn't exist" by response shape. The deck section below `CourseHeader` is a literal placeholder string ("Decks appear here once deck management lands.") — Task 4 replaces it with real deck management. Forward-references `L2-COURSE-06`; satisfies `L2-SCHOOL-06`.
- `apps/web/app/learn/courses/[courseId]/_components/course-header.tsx` — client component: title, description, status badge, and the publish/unpublish toggle. Calls `setCourseStatus(courseId, next)` inside `useTransition`, stays on that hook rather than `useActionState` because `setCourseStatus` takes positional args, not `(prevState, formData)`. The call's `ActionState` result is kept in local `useState` (cleared at the start of each toggle) and rendered with the same `text-destructive text-sm` treatment `CourseForm` uses, but only on failure (`!result.ok`) — e.g. the course was deleted in another tab/session and the scoped WHERE now matches zero rows, so the teacher sees why the button re-enabled with the status unchanged instead of nothing. Success surfaces no message: the status text next to the title already changes and `revalidatePath` refreshes the page. The toggle is the header's primary control, not a secondary settings link, because publishing is what activates existing enrolments (`L2-COURSE-09`) — a draft course is invisible to students until this button is pressed. Task 5 adds a "Students" `Link` to `/learn/courses/[courseId]/students` between the title block and the publish button — it carries the `ml-auto` that used to sit on the button, so the two sit side by side rather than being pushed apart; the failure paragraph and the button's `useTransition` behaviour are unchanged.
- `apps/web/app/learn/courses/[courseId]/_actions.ts` — deck and card authoring server actions: `createDeck`, `deleteDeck`, `createCard`, `updateCard`, `deleteCard`. `requireTeacher()` first, `schoolId` from its return only, same as `courses/_actions.ts` — but a deck/card id has no `schoolId` column of its own, so proving ownership takes a join rather than a flat `WHERE`. Two different shapes, by necessity of SQL rather than choice:
  - `deleteDeck`, `updateCard`, `deleteCard` (UPDATE/DELETE, an existing row to scope) — the mutating statement is self-scoping: the ownership check lives *inside* its own `WHERE`, as `inArray(decks.courseId, db.select({ id: courses.id }).from(courses).where(eq(courses.schoolId, teacher.schoolId)))` (`deleteDeck`) or the two-level version scoping `cards.deckId` against a `decks ⋈ courses` subselect filtered on `schoolId` (`updateCard`/`deleteCard`). `.returning()` is the only "did this match a row I'm allowed to touch" signal; an empty array is always "Deck not found."/"Card not found." — a deck or card id from another school is indistinguishable from a nonexistent one, same property `courses/_actions.ts` has. There is no separate `SELECT` before the write and no window between check and write — see "Ownership proof shape" below, now shared verbatim by both action files.
  - `createDeck`, `createCard` (INSERT, no existing row to scope) — a `SELECT` proving the parent (course, or deck→course) belongs to the teacher's school runs *before* the `INSERT`, because an `INSERT` has no row of its own for a `WHERE` clause to constrain; there is nothing to self-scope. This is the one place a preceding check is unavoidable, not a shortcut around the rule above — both actions carry a comment saying so, so a future reader does not "fix" `deleteDeck`/`updateCard`/`deleteCard` back into this shape, or ask why `createDeck`/`createCard` don't match it.
  - `revalidatePath` needs the course id, which the self-scoping DELETE/UPDATE in `deleteDeck` gets for free (`decks.courseId` is a plain column, returned directly). `updateCard`/`deleteCard` only get `cards.deckId` back from their scoped write, so both resolve `courseId` with one more plain `SELECT` *after* the mutation has already succeeded — deliberately unscoped by school, because path revalidation is not a security check and gates nothing; the security proof already happened in the write above it.
  Forward-references `L2-COURSE-06`; satisfies `L2-SCHOOL-06`.
- `apps/web/app/learn/courses/[courseId]/_components/deck-list.tsx` — client component: the decks in a course (title, link to its detail page, card count) plus an inline `createDeck` form via `useActionState`. Imports `ActionState` from the *course-level* `_actions.ts` (`../../_actions`, the shared type) and `createDeck` from the *deck-level* `_actions.ts` (`../_actions`, this task's file) — same directory, two different modules, so the two import paths matter.
- `apps/web/app/learn/courses/[courseId]/page.tsx` — the deck placeholder paragraph is gone. Queries decks with a `leftJoin` + `count(cards.id)` per deck (same no-N+1 shape as the course list's card count), scoped by `eq(decksTable.courseId, course.id)` — safe unscoped-by-school because `course` was already proven to belong to the teacher's school above — ordered by `position, title`, and renders `DeckList`.
- `apps/web/app/learn/courses/[courseId]/decks/[deckId]/page.tsx` — deck detail, server component. Proves ownership with one join (`deck` → `course`, matching `schoolId`) rather than two, because a deck id alone (no card id) is all this route has; `notFound()` on an empty result, same as the course page. Lists cards ordered `position, createdAt`, each rendered front then back through `Markdown` — the back wrapped in a muted, left-bordered `div` so it visually reads as the answer without a second click. `CardEditor` below the list. Forward-references `L2-COURSE-06`; satisfies `L2-COURSE-07`, `L2-SCHOOL-06`.
- `apps/web/app/learn/courses/[courseId]/decks/[deckId]/_components/card-editor.tsx` — client component, the new-card form (`front`/`back` `Textarea`s, `useActionState` over `createCard`). No live markdown preview: `Markdown` is written as a server component (see below) and pulling `react-markdown` into this client bundle for a preview was judged not worth the kilobytes for v1 — the saved card renders as real markdown once the page reloads via `revalidatePath`. The `front` field is a controlled input (`useState`) purely so a future preview has a value to render from; today nothing reads it back.
- `apps/web/app/learn/_components/markdown.tsx` — `<Markdown source={string} />`, a server component (no `"use client"`): `ReactMarkdown` with `remarkPlugins={[remarkGfm]}` (tables, strikethrough, task lists, autolinks), wrapped in `prose prose-sm dark:prose-invert max-w-none`. Deliberately does **not** add `rehype-raw` — `react-markdown` does not render raw HTML unless that plugin is added, so a teacher pasting `<script>` or any tag into a card body renders as literal text, not markup; adding `rehype-raw` later requires pairing it with a sanitizer (e.g. `rehype-sanitize`), not doing alone. Satisfies `L2-COURSE-07`.
- `apps/web/app/learn/courses/[courseId]/students/_actions.ts` (Task 5) — `enrollStudent`, `unenrollStudent`. This is the named enforcement point for `L2-DB-44` hazard 2: `enrollment` has no `schoolId` column, so nothing at the database level stops enrolling a student from one school into another school's course. `enrollStudent` closes that gap in application code — it re-verifies, inside the action, that the posted `courseId` resolves to a course owned by `requireTeacher()`'s school *and* that the posted `userId` is a `school_member` with `role = "student"` in that same school; neither id is trusted just because the form carried it. Enrolling twice is a no-op via `onConflictDoNothing` on the `(courseId, userId)` unique index (`L2-DB-43`), not an error — the teacher's intent ("this student should be enrolled") is already satisfied. `unenrollStudent` re-checks only the course (the enrolment row itself is the proof of the student side, so there's nothing else to re-verify) and deletes the one `enrollment` row — deliberately *not* touching a student's `card_state` rows (a plan 3 table, not created yet), so re-enrolling resumes a schedule instead of resetting it; both actions carry a comment saying so, so a future edit doesn't "simplify" the delete into a cascade. Satisfies `L2-COURSE-08`, `L2-SCHOOL-05`, `L2-SCHOOL-06`.
- `apps/web/app/learn/courses/[courseId]/students/_components/enroll-form.tsx` (Task 5) — client component, `useActionState` over `enrollStudent`. Renders nothing but a message when `candidates` is empty (every student in the school is already enrolled) rather than an empty, confusing `<select>`. Imports `ActionState` from `../../../_actions` (`courses/_actions.ts`, three levels up from `students/_components/`) — the same shared type every other authoring form in this domain uses.
- `apps/web/app/learn/courses/[courseId]/students/page.tsx` (Task 5) — roster page, server component. Same ownership-proof shape as every other course-scoped page: `and(eq(courses.id, courseId), eq(courses.schoolId, teacher.schoolId))`, `notFound()` on a miss. Two queries below that: enrolled students (`enrollment ⋈ user`, ordered by email) and candidates (`school_member ⋈ user`, filtered to `role = "student"` in the teacher's school and `notInArray` the already-enrolled ids — guarded for the empty-array case, since `notInArray(col, [])` is invalid SQL). Renders the roster as a plain list and `EnrollForm` below it. There is no remove/unenrol control wired into this page yet — `unenrollStudent` exists and is exported, but nothing in the UI calls it in this task; it is reachable today only by direct action call (e.g. from a future page, or a test). That gap is intentional scope, not an oversight: the brief for this task ships enrol-only UI.
- `apps/web/app/learn/_lib/uploads.ts` (Task 6) — pure-ish storage helpers: `UPLOAD_DIR` (env override or `./shared/uploads`, `L2-INF-18`), `safeUploadName(originalName)` (a `crypto.randomUUID()` name with a permitted extension — png/jpg/jpeg→jpg/gif/webp — or `null` for anything else, **SVG deliberately excluded** since it can carry script and we serve from our own origin), `uploadPath(name)` (throws on any `name` containing `/`, `\`, or `..`, so it can only ever resolve to a bare file directly under `UPLOAD_DIR`), and `matchesFileSignature(bytes, ext)` (fix round 1: checks the file's leading bytes — PNG `89 50 4E 47`, JPEG `FF D8 FF`, GIF `47 49 46 38`, WebP `52 49 46 46`…`57 45 42 50` at offset 8 — against the extension the name was already assigned; an extension alone is just a string the uploader chose, nothing upstream reads the bytes otherwise). Both the upload action and the serving route depend on this module for the one property that matters: the stored/served name is never the client-supplied one, and (as of fix round 1) the stored bytes actually are what they claim to be. Forward-satisfies `L2-COURSE-10`.
- `apps/web/app/learn/_lib/uploads.test.ts` (Task 6, extended fix round 1) — unit tests for `safeUploadName` (extension allow-list including case-insensitivity, SVG/HTML/JS/no-extension refusal, `../../etc/passwd.png` producing a name with no `/` or `..` in it, two calls for the same input producing distinct names), `uploadPath` (throws on a decoded traversal string, throws on a name carrying `/` or `\`, throws on the still-URL-encoded `..%2F..%2Fpackage.json` form as a defense-in-depth property — not a claim that string reaches this function in production; see the comment in the file and the route-level test below for the real boundary — resolves a clean bare name to a path under `UPLOAD_DIR`), and `matchesFileSignature` (real magic bytes accepted per type; **SVG or HTML bytes wearing a `.png` name refused** — the case that matters; mismatched-but-permitted type pairs refused; too-short input refused; an unrecognized extension refused). This is the proof of the traversal and content-spoofing defences in this environment — there is no Docker/Postgres here to bring up a dev server and run the brief's `curl -i '.../..%2F..%2Fpackage.json'` check by hand; see "Manual verification" below for what that check would additionally confirm end-to-end, and `route.test.ts` (below) for the route-level version of the traversal proof.
- `apps/web/app/learn/courses/[courseId]/decks/[deckId]/_upload-actions.ts` (Task 6, extended fix round 1) — `uploadCardImage(prev, formData)`, the `file`-field server action. `requireTeacher()` first (teacher-only, same gate as every other authoring action), then rejects a missing/empty file, then a file over `MAX_BYTES` (2 MB — the *user-facing* cap; see below), then a disallowed extension (via `safeUploadName` returning `null`), then (fix round 1) a content/extension mismatch via `matchesFileSignature` — in that order, cheapest check first. `mkdir(UPLOAD_DIR, { recursive: true })` before every write rather than once at startup, since a fresh checkout or a droplet with an unprovisioned `shared/uploads` should not 500 on the first upload. Returns `{ ok: true, message, url: "/api/learn/uploads/<name>" }` on success — the message itself already contains the pasteable `![](url)` markdown, which `ImageUpload` re-derives from `url` rather than parsing out of `message`. No `revalidatePath` call: an upload doesn't change any page's rendered data by itself, only the follow-on paste into a card front/back does (and `createCard`/`updateCard` already revalidate). **Fix round 1, the 2 MB cap was unreachable:** Next's server-action body limit defaults to 1 MB, below `MAX_BYTES`, so a 1.5 MB file was rejected by the framework — an unfriendly error — before this function ever ran. Fixed in `apps/web/next.config.ts` by raising `experimental.serverActions.bodySizeLimit` to `"3mb"`, deliberately *above* `MAX_BYTES` rather than lowering `MAX_BYTES` to match the old 1 MB ceiling: the framework limit covers the whole multipart body (fields + boundaries + file), so it needs headroom above the 2 MB file cap to still let this function's own check (with this function's own message) fire first. Both sites carry a comment that the two numbers must stay apart. `bodySizeLimit` is global — it raises the ceiling for every server action in the app, not only this one; acceptable here because every other action in this app posts small text fields.
- `apps/web/app/api/learn/uploads/[name]/route.ts` (Task 6) — `GET`, gated on `auth()` returning a session (401 if not) rather than `requireMembership()`/`requireTeacher()`. This is deliberate, not a shortcut: images are requested by the browser as markdown-rendered `<img>` sub-resources on every study/authoring screen that shows a card, and a per-image school/enrolment check would be one extra query per image per render. The stored name is an unguessable UUID (`safeUploadName`), so knowing the URL already implies having seen it somewhere inside the app; a bare session is the meaningful boundary, membership is not. `uploadPath(name)` is called inside a `try`/`catch` — a name that fails the bare-filename check (traversal, separators) throws, caught and turned into 404, identical to a name that parses fine but doesn't exist on disk (`readFile` catch) — this same catch is also what stops a name carrying a null byte (it passes `uploadPath`'s own checks, since a null byte is not `/`, `\` or `..`; Node's `fs` layer refuses a path containing one, and that refusal lands in this catch too, proven directly by `route.test.ts`). Both paths return the same 404, so a traversal attempt is indistinguishable from "file not found" — no signal either way about what exists outside `UPLOAD_DIR`. Extension→`Content-Type` is a fixed map (`png`/`jpg`/`gif`/`webp`), not read from the file or a stored column — an unknown extension also 404s before ever touching the filesystem. `Cache-Control: private, max-age=31536000, immutable` is safe because the name is never reused once "distinct name per upload" holds. `export const runtime = "nodejs"` because `node:fs/promises` needs it — the edge runtime has no filesystem. Satisfies `L2-COURSE-10`.
- `apps/web/app/api/learn/uploads/[name]/route.test.ts` (Task 6, fix round 1) — route-level test, `GET` called directly with a crafted `params` promise and `auth` mocked (module-level `vi.mock("@/app/_lib/auth", …)`, same pattern as `apps/web/app/backflip/(protected)/connect/_actions.test.ts` — avoids pulling in the real NextAuth/DB config chain). Asserts: 401 with no session (before the name is even looked at); 404 for a traversal name (`../../package.json`); 404 for a name with a path separator; 404 for an unrecognized extension; 404 for a name carrying a null byte (the case that proves the `readFile` catch, not `uploadPath`, is the backstop there); 404 for a well-formed but nonexistent name. This is the actual reproduction of the brief's `curl -i '.../..%2F..%2Fpackage.json'` check, run against the handler directly rather than over HTTP — the environment has no dev server to curl against, but this exercises the identical code path from the identical entry point (a `params` promise, exactly what Next hands `GET`).
- `apps/web/app/learn/courses/[courseId]/decks/[deckId]/_components/image-upload.tsx` (Task 6) — client component, `useActionState` over `uploadCardImage`. This exists specifically because the brief's own Step 8 stopped short of it ("or test the action directly for now") — the same half-wired shape a plan-5 review already flagged once, so this task ships a real control rather than repeating it. A file `<Input type="file" accept="image/png,image/jpeg,image/gif,image/webp">` (the `accept` attribute is a UX hint only — the server action is the actual gate, `accept` can't be trusted and isn't treated as one) plus a submit button; on success, the `url` from state renders as a readonly `<Input value={`![](${state.url})`}>` with `onFocus` select-all, so the teacher can click once and copy — deliberately not a live "insert into the field below" wiring, since `CardEditor`'s textareas are a separate form/component tree and reaching into it would need lifted state neither form currently has. On failure, `state.message` renders with the same `text-destructive text-sm` treatment `CardEditor`/`CourseForm`/`EnrollForm` all use. Rendered on the deck page under a `<h2>Get an image URL to paste</h2>`, below `CardEditor`, separated by a `border-t` — a different job (producing a snippet) from the card form's job (saving markdown text), so it is its own form rather than a field folded into `CardEditor`.
- `apps/web/app/learn/courses/[courseId]/decks/[deckId]/page.tsx` (Task 6) — imports and renders `ImageUpload` below `CardEditor`, under an `<h2 className="text-sm font-medium">Get an image URL to paste</h2>` inside a `border-t pt-6` block so it reads as a distinct sub-section of the deck page rather than part of the card form above it.
- **`prose` dependency fix (not in the original task brief):** the brief's `markdown.tsx` used `prose prose-sm dark:prose-invert` assuming `@tailwindcss/typography` was already available. It was not — nothing in the repo depended on it and no component used `prose` before this task. Without it those classes are dead: the build and tests pass either way, but every card renders as unstyled run-together text. Fixed by adding `@tailwindcss/typography` as a `packages/ui` devDependency and loading it in `packages/ui/src/styles/globals.css` via `@plugin "@tailwindcss/typography";` (Tailwind v4 is CSS-first here — no `tailwind.config.js` to register a plugin in). See `docs/notes/ui.md` and `L2-UI-03` for the shared-package side of this; verified by building `web` and grepping the built CSS chunk for `.prose`/`dark:prose-invert` selectors and the `--tw-prose-*` variable set, not just a passing build.

## Ownership proof shape
Every UPDATE/DELETE in `courses/_actions.ts` and `[courseId]/_actions.ts` follows the same two-part shape: resolve the teacher server-side first (`const teacher = await requireTeacher()`), then scope the mutating statement's own `WHERE` to the teacher's school — `and(eq(courses.id, id), eq(courses.schoolId, teacher.schoolId))` directly in `courses/_actions.ts` (the row itself carries `schoolId`); `inArray(<fk column>, <subselect filtered on schoolId>)` in `[courseId]/_actions.ts` (`deleteDeck`, `updateCard`, `deleteCard`), because a deck/card row has no `schoolId` column of its own to filter on directly. Both are deliberate over fetch-then-check (a preceding `SELECT`, compare `schoolId`, then an unscoped write): a single scoped `UPDATE`/`DELETE ... WHERE ... RETURNING` is one round trip with no window between the check and the write, and an empty `.returning()` array is the one signal needed to tell "not found" from "found but wrong school" apart — both report identically as "not found", so a teacher never learns whether an id belongs to a different school.

INSERTs (`createCourse`; `createDeck`, `createCard`) are the deliberate exception, not a lapse: there is no existing row for an INSERT's `WHERE` to constrain, so the only way to prove the parent (school, or course/deck) belongs to the teacher is a `SELECT` before the write. This was already true of `createCourse`; `createDeck`/`createCard` carry it for the same structural reason and each has a comment saying so, precisely so a future reader does not read the newer UPDATE/DELETE shape and "fix" the two INSERTs — or the reverse — into matching it.

## `schoolId` source, restated
`createCourse` and every scoped update/delete take `schoolId` from `requireTeacher()`'s return, never a second `getCurrentSchoolId()` call — the single most load-bearing rule carried over from the school domain's review (`L2-SCHOOL-11`, `docs/notes/school.md`). `getCurrentSchoolId()` has no caller in this file.

## Test coverage: what is proven now, and what still isn't
`apps/web/e2e/authoring.spec.ts` (this task) closes the "never run against a real database with two schools" half of the old gap. It drives the whole happy path through the UI as `OWNER` — create a course, add a deck (asserted at 0 cards), open the deck and add a card with `**bold**` markdown on the front (asserted as an actual `<strong>` element, not literal asterisks — the check that would catch the typography plugin or the renderer silently failing), enrol a student, publish the course (status flips) — and then proves cross-school isolation against a real second school (`RIVAL_SCHOOL`) and second teacher (`OUTSIDER`), seeded directly in `global-setup.ts`, not through the UI:
- `OWNER`'s course list never contains `OUTSIDER`'s course title.
- `OWNER` navigating straight to `OUTSIDER`'s course URL, and to a deck seeded under it, gets a real 404 (`response.status() === 404`, same URL, no redirect) — not a rendered page with data withheld.
- The reverse direction is also asserted: `OUTSIDER` signed in sees and can open their own course. Without this, the 404s above would be equally consistent with a broken route as with real isolation — proving the reverse direction is what rules that out.

**What this still does not cover, honestly:**
- `updateCourse` and `deleteCourse` are not driven by any UI in this task — the course/deck forms only create — so their scoped `WHERE` clauses remain unexercised by an automated test, proven only by code shape (`L2-COURSE-06`) and by the identical pattern the exercised actions use.
- `enrollStudent`'s own cross-school check (`L2-DB-44` hazard 2 — a course from one school, a student from another) is not separately exercised: the spec only enrols a same-school student. The isolation proof covers course/deck *reach* (list, detail, deck routes), not the enrolment action's own two-sided school check.
- `deleteDeck`, `updateCard`, `deleteCard`, `unenrollStudent` are still untested by anything — no UI control calls `unenrollStudent` yet (`docs/notes/courses.md`'s route inventory notes this above), and the others have no exercised path either.
- The auth gate itself (`requireTeacher()` rejecting a signed-in non-member or a student) is covered generally by `learn.spec.ts` (`L2-SCHOOL-08`), not specifically for every authoring route.

So: cross-school isolation for course/deck reach is no longer "asserted by the code shape and by reasoning" — it has been run against a real database and held. The remaining gaps above are narrower and named, not a blanket "no coverage" claim.

## Known repo-wide weakness: `String()` coercion ahead of schema validation
Every `formData.get(...)` in `_actions.ts` is wrapped in `String(...)` before `courseSchema.safeParse`. If a `File` is posted under a text field name (e.g. `title`), `formData.get("title")` returns a `File` object, `String(file)` coerces it to `"[object File]"`, and that string then validates as ordinary text instead of being rejected as the wrong input type. This is not specific to this task — it is the pre-existing convention used verbatim in `apps/web/app/backflip/(protected)/users/_actions.ts`, `apps/web/app/backflip/(protected)/school/_actions.ts`, and the auth actions, so it was kept here rather than deviated from for one module. Recorded here as a findable, repo-wide input-handling weakness for whoever decides to fix it everywhere at once (e.g. by checking `typeof value === "string"` before coercing).

## Route inventory
All under `apps/web/app/learn`, all server components except where noted, all gated by `requireTeacher()` (or `requireMembership()` at the `/learn` shell level):
- `/learn` — shell/landing (`page.tsx`, `layout.tsx`, `LearnNav`).
- `/learn/courses` — teacher's course list + `CourseForm` (create).
- `/learn/courses/[courseId]` — course detail: `CourseHeader` (title, status, publish toggle, Students link) + `DeckList` (decks, inline create).
- `/learn/courses/[courseId]/decks/[deckId]` — deck detail: cards (front/back rendered as markdown) + `CardEditor` (inline create) + `ImageUpload` (Task 6, get an image URL to paste).
- `/learn/courses/[courseId]/students` (Task 5) — roster: enrolled students, `EnrollForm` (candidate dropdown + enrol).
- `GET /api/learn/uploads/[name]` (Task 6) — the one route in this domain that isn't a page: serves an uploaded card image. Gated on a session (`auth()`), not `requireTeacher()`/`requireMembership()` — see the file-map entry above for why.

Server actions, one `_actions.ts` per directory level: `courses/_actions.ts` (`createCourse`, `updateCourse`, `setCourseStatus`, `deleteCourse`), `courses/[courseId]/_actions.ts` (`createDeck`, `deleteDeck`, `createCard`, `updateCard`, `deleteCard`), `courses/[courseId]/students/_actions.ts` (`enrollStudent`, `unenrollStudent`, Task 5), `courses/[courseId]/decks/[deckId]/_upload-actions.ts` (`uploadCardImage`, Task 6 — a separate file from the deck-level `_actions.ts` at the parent directory, since it's a distinct concern with its own `MAX_BYTES`/`UploadState`, not another deck/card CRUD action).

## Manual verification (Task 5, enrolment)
Not run in this environment — recorded here as the steps to reproduce, per the task brief, not as a claim they were executed:
1. `corepack yarn dev`; from `/backflip/school`, invite a second person as a `student` in the same school.
2. As the teacher, open a course → **Students**. Enrol the new student from the dropdown.
3. Confirm they move out of the dropdown (no longer a candidate) and into the roster list.
4. Submit the same enrolment a second time (e.g. browser back/forward, resubmit). Expect `{ ok: true, message: "Student enrolled." }` again, no error, and `select count(*) from enrollment` unchanged — `onConflictDoNothing` on `(courseId, userId)` makes the retry a no-op.

## Manual verification (Task 6, card images)
Not run by the implementer in this environment — no Docker/Postgres available here, so no dev server could be brought up. `apps/web/app/learn/_lib/uploads.test.ts` and `apps/web/app/api/learn/uploads/[name]/route.test.ts` prove the traversal and content-spoofing defences at the unit/route level (see the file-map entries above). Fix round 1's review did exercise the traversal defence against something with a real filesystem — double URL-encoding, overlong UTF-8, `%5C`, null bytes, and unicode lookalikes were all tried, all rejected, and a traversal attempt was confirmed indistinguishable (identical 404) from a missing file. The steps below are what a full dev-server walkthrough would additionally confirm end-to-end (the paste-into-a-card and renders-on-page path, which no unit test covers) and are recorded as reproduction steps, not as executed by the implementer:
1. `corepack yarn dev`; sign in as a teacher, open a deck page, use the "Get an image URL to paste" form to upload a PNG.
2. Confirm the response is `{ ok: true, url: "/api/learn/uploads/<uuid>.png" }`, the readonly field shows `![](/api/learn/uploads/<uuid>.png)`, and the file exists on disk under `shared/uploads/` (or `UPLOAD_DIR` if set).
3. Paste that markdown into a card's front, save, and confirm the image renders on the deck page.
4. Sign out (or use an incognito window) and request the URL directly. Expect `401`.
5. `curl -i 'http://localhost:3070/api/learn/uploads/..%2F..%2Fpackage.json'` (signed-in session cookie attached, or expect 401 first if not). Expect `404`, never `package.json`'s contents.
6. Upload a file just over 2 MB. Expect `{ ok: false, message: "Images must be 2 MB or smaller." }` from `uploadCardImage` itself, not a framework-level error — this is the fix round 1 regression check for the `bodySizeLimit`/`MAX_BYTES` gap.
7. Rename an SVG or HTML file to `foo.png` and upload it. Expect `{ ok: false, message: "That file's contents don't match a PNG, JPEG, GIF or WebP image." }`.

## State
Schemas + unit tests + course CRUD actions + the course list/detail pages and publish toggle, plus deck and card authoring (Task 4: deck/card actions, deck list, deck detail page, card editor, and the `Markdown` renderer used by the deck page), plus enrolment (Task 5: `enrollStudent`/`unenrollStudent`, the students roster page, `EnrollForm`, and the Students link on `CourseHeader`), plus card images (Task 6: `@/app/learn/_lib/uploads`, `uploadCardImage`, the `GET /api/learn/uploads/[name]` route, and `ImageUpload` wired onto the deck page) are done. `docs/contracts/courses.md` now exists, so the `L2-COURSE-01..08` forward-references from Tasks 1–4 resolve to real clauses; `L2-COURSE-09`/`L2-COURSE-10` and the contract's Invariants/Errors/Acceptance sections are new (Task 5 and Task 6 respectively). The course detail page's deck placeholder is gone, replaced by `DeckList`. No study/review screen yet — `Markdown` renders a card's front and back on the teacher-facing deck page only; the student review flow is a later task in this plan. The roster page has no remove/unenrol control wired into its UI — `unenrollStudent` exists per the interface spec but nothing calls it yet. See the test-coverage gap above — it still applies, and now also covers `enrollStudent`/`unenrollStudent`: no test drives `requireTeacher()`'s redirect from these routes, the cross-school course/student checks, or the `onConflictDoNothing` idempotency, against a real database. Card images add one narrower, closed gap of the same shape: `uploadCardImage`'s `requireTeacher()` gate is asserted by code shape but not exercised against a running app; the serving route's session gate and traversal/null-byte handling now IS exercised at the route level (`route.test.ts`), and the traversal defence specifically was reviewed against a real filesystem in fix round 1 (double-encoding, overlong UTF-8, `%5C`, null bytes, unicode lookalikes — all rejected) — see "Manual verification (Task 6, card images)" above.

## Fix round 1 (post-review)
Three findings, all fixed in the same commit as this note:
1. **2 MB cap was unreachable.** Next's server-action default body limit (1 MB) sat below `MAX_BYTES` (2 MB), so the framework rejected anything over 1 MB before `uploadCardImage` ran, and the teacher never saw the app's own message. Fixed by raising `experimental.serverActions.bodySizeLimit` to `"3mb"` in `apps/web/next.config.ts` — deliberately above, not equal to, `MAX_BYTES`, since the framework limit covers the whole multipart body and a 2 MB file plus form overhead can exceed a 2 MB ceiling. Both `next.config.ts` and `_upload-actions.ts` carry a comment that the two numbers must stay apart. This raises the body limit for every server action in the app, not only this one — accepted because every other action here posts small text fields.
2. **Nothing inspected the bytes.** An SVG/HTML file renamed to `foo.png` would have been stored and served as `image/png`, relying entirely on the repo-wide `X-Content-Type-Options: nosniff` header (`apps/web/next.config.ts`, a file this task otherwise never touches) to stop a browser from executing it. Fixed by adding `matchesFileSignature` to `uploads.ts` (magic-byte check against the claimed extension, called from `uploadCardImage` before the file is written) and recording in `L2-COURSE-10` that `nosniff` is load-bearing for this defence, not decorative.
3. **A misleading test, and an untested real layer.** The original `..%2F..%2Fpackage.json` case in `uploads.test.ts` only passed because the literal `..` substring survives encoding — it never proved what the route receives in production (Next decodes route params first), and nothing tested the route itself. Fixed by relabeling that unit test as a defense-in-depth check (comment updated) and adding `route.test.ts`, which calls `GET` directly with a crafted `params` promise — the actual reproduction of the brief's curl check — covering traversal, a path separator, an unknown extension, a null byte, and the no-session case.
